// src/theme.ts
import { createTheme } from '@mui/material/styles';

// Create a theme instance with your Google Font
const theme = createTheme({
  typography: {
    fontFamily: "'Play', sans-serif", // Replace with your chosen Google Font
  },
});

export default theme;
